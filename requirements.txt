# Upper bounds matter here: awpy IS the parser, and the whole contract of
# src/extract.py is awpy's returned frame shape and its ct/t side casing — both of
# which changed across its 1.x -> 2.x boundary. An unpinned major would either fail
# loudly at parse time or, worse, write differently-shaped rows into the same
# data/crosshair.db that already holds matches parsed by the old version.
#
# These are compatibility ceilings, not the exact resolution that produced the
# existing corpus — that version is not recorded anywhere. After rebuilding the venv,
# run `.venv/bin/pip freeze > requirements.lock` so it is recorded from then on.

# demo parsing
awpy>=2.0.2,<3
polars>=1.0.0,<2

# data + ML
pandas>=2.0.0,<3
numpy>=1.24.0,<3
scikit-learn>=1.3.0,<2
lightgbm>=4.0.0,<5

# scraping / download
requests>=2.31.0,<3
playwright>=1.40.0,<2
playwright-stealth>=1.0.6,<2   # declared but not imported anywhere — see docs/reviews/
zstandard>=0.21.0,<1

# LLM — declared but not imported anywhere in src/. See docs/reviews/.
openai>=2.31.0,<3
